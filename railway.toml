[build]
builder = "nixpacks"
buildCommand = "npm install && node scripts/generate-assets.cjs && npm run build"

[deploy]
startCommand = "mkdir -p /data && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
