[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "mkdir -p /data && node scripts/decode-assets.cjs && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
